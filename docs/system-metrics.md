# System metrics collector

This lightweight collector publishes basic host-level telemetry signals into the same InfluxDB bucket used by the main pipeline.

It is intended as a simple extension that demonstrates how the project can evolve beyond sample data into host monitoring.
