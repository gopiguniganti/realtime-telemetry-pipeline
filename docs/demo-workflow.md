# Demo workflow

1. Start the local stack with Docker Compose.
2. Run the ingestion script to publish sample telemetry points.
3. Open Grafana and connect to the InfluxDB bucket.
4. Visualize the incoming data with a simple time-series graph.

This workflow demonstrates the end-to-end flow from ingestion to observation.
